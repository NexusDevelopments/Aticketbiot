{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm --workspace server run prisma:generate && npm --workspace client run build"
  },
  "deploy": {
    "startCommand": "npm --workspace server run prisma:deploy && node server/src/index.js"
  }
}
